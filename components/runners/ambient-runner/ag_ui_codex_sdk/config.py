"""
Configuration constants for Codex SDK adapter.

Defines defaults and configuration options for the Codex SDK integration.
"""

# Special tool name for state management
STATE_MANAGEMENT_TOOL_NAME = "ag_ui_update_state"

# Default Codex model
DEFAULT_MODEL = "gpt-5.1-codex"

# Default approval mode for Codex sessions (never requires approval)
DEFAULT_APPROVAL_MODE = "never"

# Default sandbox mode — allows writes within the workspace
DEFAULT_SANDBOX_MODE = "workspace-write"
